---
import { themeConfig } from '~/.config'
import { getPathFromCategory } from '~/utils'

interface Props {
  category: string
}

const { category } = Astro.props
const href = `/categories/${getPathFromCategory(category, themeConfig.site.categoryMap)}`
---

<a href={href}>{category}</a>
