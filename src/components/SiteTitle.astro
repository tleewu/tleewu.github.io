---
import { themeConfig } from '~/.config'

const { title, avatar } = themeConfig.site
---

<div class="branding">
  {avatar && <img class="branding__avatar" src={avatar} alt="" width="48" height="48" />}
  <a class="branding__name" href="/">{title}</a>
</div>
